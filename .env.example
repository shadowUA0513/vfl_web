# Base URL of the VFL API as seen by the browser.
# Default "/api/v1" goes through the Vite dev proxy (or your production reverse proxy).
VITE_API_URL=/api/v1

# Dev-only: where the Vite proxy forwards /api requests.
VITE_API_PROXY_TARGET=http://46.62.230.64:8081
